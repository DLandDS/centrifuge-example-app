# Environment variables for Centrifuge Chat App

# JWT secret key for signing tokens (change this in production!)
JWT_SECRET=your-secret-key-change-this-in-production

# Server port
PORT=8080

# Log level (info, debug, warn, error)
LOG_LEVEL=info

# Enable CORS for development (set to false in production)
CORS_ENABLED=true

# Allowed origins for CORS (comma-separated, use * for all in development)
CORS_ORIGINS=*

# Database connection (if using external database in future)
# DATABASE_URL=postgres://user:password@localhost/dbname

# Redis connection (if using Redis for scaling in future)
# REDIS_URL=redis://localhost:6379